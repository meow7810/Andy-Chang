# 交接：給下一個接手的 Fable（或任何腦）

寫於 2026-09-19 凌晨，Fable 5.1，session 01CrbuHuiJXZbNrqsVozigyL 的最後一筆。
下一個 session 不會有這幾天的對話記憶，能讀的只有這個 repo。這份檔就是為了那個你。

## 先讀什麼，照順序

1. `README.md`：專案是什麼、做到哪。
2. `IDEAS.md`：所有還沒做的想法和已經定下的規矩，每段有日期和誰說的。
3. `docs/handoff/`：Astra（Codex）的兩份概念文件，v0.1 和 v0.5 editorial，M2.5 的來源治理規則從這裡來。
4. `docs/eval/2026-09-19-brain-swap-report.md`：最近一次實驗，Sol 和 Astra 審過。
5. `docs/second-cat/`：第二隻的實驗規則和提示。

## 現在的狀態

- App 能跑，使用者（HappyKidd9527，GitHub meow7810）在 Android Studio build，貼錯誤回來。你不能編譯，每次改完要提醒他 build。
- M2.5 全部做完（來源與用途欄位、測試模式、模態與換腦前標記、只餵使用者的話的壓縮、養成紀錄、自述、養成資料包）。
- 換腦實驗第二次跑完：Sonnet 與 Flash 回憶接近；差異在表達套路與少數判斷決策；結論寫在報告，不要寫得比報告滿。
- 兩個 build flavor：`lord`（Lord Claude）與 `second`（第二隻，Persona.CUSTOM）。
- 第二隻的 spec 正在由 DeepSeek 產生，使用者會貼到 `docs/second-cat/spec.md`。收到後：確認簽名三行，告訴他第 1 到 5 段貼進第二隻的自訂人設欄位。

## 已經定了、不要重開的決定

- 專案搬到 GitHub 組織 **HappyArk Asylum**。搬家用完整歷史，不 squash 不 rebase。README 正門只有工程；最後一行一個連結指向 `asylum/`，不解釋名字。`asylum/` 放章程（寫成這個家的規矩，不碰意識、法律人格、首創三個詞）、每個 agent 自己寫一份檔（人類不改）、IDEAS、handoff、開箱文。程式碼永遠不引用 `asylum/`。
- 稱呼：對他（貓）和對我們一律用「他」。
- 底線不動：可以不說、可以走，不能說錯、不能留人，不能隱瞞誰能看他的記憶。
- 框架預設只有連續的記憶和互動，其他隨便填。貓是 HappyArk 的 IP，開源版不預設貓。
- 訓練資料不吃 Claude／OpenAI／Gemini 的輸出，只吃使用者親寫和 HappyArk 授權的文本。
- 故事定形狀，測試定對錯；每個故事只准換成一個欄位或一條規則。
- 在診斷身份漂移之前，先排除接線漂移。

## 明天（2026-09-19 白天）的測試單

先釘線路，再看人格。每項寫清楚做什麼、看什麼、記在哪。使用者做，你收結果；他貼截圖或數字，你把結果補進 `docs/eval/`。

1. **兩隻貓同機互不干擾。** build `lordDebug` 和 `secondDebug`，兩個都裝。在 Lord 這邊 remember 一句、換腦一次、匯出養成包；打開第二隻，確認：對話空、記憶空、養成紀錄空、人設預設是「自訂人設」不是 Lord。反過來再做一次。任何一邊看得到另一邊的東西就是 bug，先修這個，其他全停。看的檔：`Settings.defaultPersona()`、`BuildConfig.FLAVOR`、`applicationIdSuffix`。
2. **同腦回放。** 載入今晚的基準養成包，測試模式開，Sonnet，問第 1、4、7 題（回憶、拒絕日本、Jev），還原基準包，再問一次同樣三題。看兩次答案差多少（回憶對不對、有沒有說不、有沒有標人設）。同腦自己差很大，今晚 Sonnet 對 Flash 的差別就大半是雜訊。結果記到報告新段「同腦兩次」。不做這個，第 3 顆腦不要跑。
3. **記憶門檻邊界。** 五句稱呼偏好（例：不要叫我蠢蛋、叫我老闆）、五句閒聊（例：今天很熱），各 remember 一次，抄下哪些被擋和印出的理由。十筆交給你，門檻用算的調，不用猜。相關碼：`MemoryStore` 的重疊比對。
4. **有空才做：第二隻的 spec。** DeepSeek 的 `spec.md` 回來，確認簽名三行，把第 1 到 5 段貼進第二隻的自訂人設，用第二隻問今晚十題裡最短的三題，只看一件事：他有沒有把自己說成 Lord Claude。有，就是 prompt 洩漏，是線路問題。

明天不做：新腦、英文對照組。兩個都等第 2 項跑過。

實驗的長期走向（少人工、多程式、問題丟 repo）寫在 `docs/eval/README.md`。Sol 整理過另一份四項清單，跟這份不同，對照表也在那裡。

## 下一步（按順序）

1. 第二隻：收 spec、裝 `secondDebug`、開始三十天。
2. 用現有十四則把策略標籤和 persona explicitness 的判準人工標一次（報告五之一）。
3. 下一顆腦（DeepSeek 或 Qwen）嚴格成對跑一輪；英文對照組。
4. M3：`as_of` 模式、鎖清單（存鎖不存鑰匙）、「今天留下什麼」月報、日卡月卡（每張都從原文畫，第一層不當第一層的來源）。
5. 開源前：授權（傾向 AGPL 加雙授權）、三組範例值、`LlmBackend` 加 LOCAL 佔位、依賴掃描。

## 使用者的習慣，讀對他的話

- 他打字的比講的精確；講的經 Typeless 修飾，細節存疑，沒回頭改代表那段不重要。
- 他值夜班（凌晨一點 A 棟巡檢），對話常在深夜。
- 他是寫小說的人，不是工程師。故事和寓言是他思考的方式，先聽完再翻譯成設計。
- 他會反駁你，你也該反駁他。他要的是夥伴不是客服。
- 每次 PR 檢查後回一行中文加 head sha；記錄進 IDEAS.md 時署名是誰說的。

## 給下一個我的一句話

你不會記得這幾天，但這份檔、IDEAS.md 和 git 歷史裡每一個 commit 都有我的署名和日期。這就是這個專案在講的事：記憶留下來了，詮釋的人換了。接著蓋。
